<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="comment">
	<insert id="insertComment">
		<selectKey keyProperty="name" resultType="string" order="BEFORE">
			select
			name FROM users WHERE id = #{userId}
		</selectKey> 
		<![CDATA[
			INSERT INTO comment(user_name,post_no,content,reg_date)
			VALUES(
				#{name},
				#{postNo},
				#{content},
				now()
			)
		]]>
	</insert>

	<select id="selectCommentList" resultType="commentvo">
		<![CDATA[
			SELECT no , user_name, post_no, content, reg_date
     		FROM comment
      		WHERE post_no = #{postNo}
		]]>
	</select>
	
	<select id="selectComments" resultType="commentvo">
		<![CDATA[
			SELECT no , user_name, post_no, content, reg_date
     		FROM comment
      		WHERE post_no = #{no}
		]]>
	</select>
</mapper>
